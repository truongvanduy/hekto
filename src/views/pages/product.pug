extends ../layout/layout

block vars
  -title = 'Hekto: Products'

block container 
  +mainHeader("Products")

block scripts 
  script(src="../js/app.js")
  script.
    app.renderProductList();
    app.start();